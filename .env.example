# Copy to .env (gitignored). All entries optional — the demo runs offline.
# IPQualityScore key for live vendor enrichment (vendor/enrich.py --live)
IPQS_API_KEY=
# LLM live modes: either a Claude Code-compatible CLI on PATH...
CLAUDE_CLI_BIN=claude
# ...or an API key for the anthropic SDK backend (LLM_BACKEND=api)
ANTHROPIC_API_KEY=
